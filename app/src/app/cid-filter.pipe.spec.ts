import { CidFilterPipe } from './cid-filter.pipe';

describe('CidFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CidFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
